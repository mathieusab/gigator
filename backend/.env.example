# Backend (Express) environment

# Port the backend listens on
PORT=3000

# Gmail Proxy OAuth client credentials (server-side)
# Used for token exchange or Gmail API access, depending on implementation.
GMAIL_PROXY_CLIENT_ID=
GMAIL_PROXY_CLIENT_SECRET=
